---
import { SITE } from "@consts";
---

<section
  class="grid grid-cols-1 md:grid-cols-2 items-center container mx-auto min-h-screen p-6 md:p-0 gap-8 pt-18"
>
  <h1 class="text-[clamp(6rem,20vw,12.5rem)] uppercase font-bold">
    {SITE.TITLE}
  </h1>
  <div class="space-y-8">
    <p class="text-2xl text-secondary">
      A full-stack developer with a strong focus on building fast, accessible,
      and maintainable web applications.
    </p>
    <div class="flex flex-col md:flex-row gap-2.5">
      <button
        class="inline-flex items-center justify-center bg-primary rounded-4xl py-6 px-26 cursor-pointer hover:bg-background hover:outline-1 hover:outline-dashed hover:outline-secondary hover:text-foreground"
        >View Work</button
      >
      <button
        class="inline-flex items-center justify-center bg-foreground text-background rounded-4xl py-6 px-26 cursor-pointer hover:bg-background hover:outline-1 hover:outline-dashed hover:outline-secondary hover:text-foreground"
        >Hire me</button
      >
    </div>
  </div>
</section>
